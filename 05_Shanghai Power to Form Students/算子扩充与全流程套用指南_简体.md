# 算子增加范例(简体)

改 4 个文件,新算子就套用到全流程(05)。以下用 `twist`(权力漩涡)为例,照抄改名即可。

---

## 契约

```python
rec = {"geom": <shapely, 米 EPSG:32651>, "h": <float 米>,
       "sh": "state|developer|resident|unknown|informal", "frozen": <bool>}
```

- 纯函数 `recs -> recs`;先复制 `out=[dict(r) for r in recs]`,别动原始。
- 只动 `r["sh"] in target and not r.get("frozen")`。
- GFA ∝ `geom.area × h`。守恒=缩面积补高度;加量=只抬高度。

---

## ① `engine/operators.py` — 贴函数 + `OPS` 加一行

```python
def twist(recs, target, deg_per_100m=12.0):
    """权力漩涡:目标楼 footprint 依到全域 GFA 重心的距离旋转(越远转越多)。
    旋转不改面积、高度不变 → 单栋 GFA 守恒。"""
    import math
    import shapely.affinity as aff
    out = [dict(r) for r in recs]
    A = sum(r["geom"].area * r["h"] for r in out) or 1.0
    Px = sum(r["geom"].centroid.x * r["geom"].area * r["h"] for r in out) / A
    Py = sum(r["geom"].centroid.y * r["geom"].area * r["h"] for r in out) / A
    for r in out:
        if r["sh"] in target and not r.get("frozen"):
            d = math.hypot(r["geom"].centroid.x - Px, r["geom"].centroid.y - Py)
            r["geom"] = aff.rotate(r["geom"], deg_per_100m * d / 100.0, origin="centroid")
    return out
```

文件尾:

```python
OPS = {..., "scale_graded": scale_graded, "twist": twist}   # ← 加 twist
```

> 只 `ops.register("twist", twist)` = runtime 登记,06 是独立进程 → `KeyError`。必须永久写进 `OPS`。

---

## ② `regimes.yaml` — 加体制

```yaml
power_vortex:
  label: 权力漩涡
  desc: 先锁政府,居民/开发商楼依到权力重心距离旋转,最后小幅加密。
  feature: 旋转错落
  steps:
    - { op: freeze, who: [state] }
    - { op: twist, target: [resident, developer], deg_per_100m: 15 }
    - { op: densify, target: [resident, developer], far_gain: 1.3, cap_m: 300 }
```

---

## ③ `06_AI Render Students/config.yaml` — 两处

```yaml
regimes:
  - current
  - developer_led
  - state_led
  - resident_self_build
  - shared
  - power_vortex # ← 加名字,否则 06 不渲染

prompts:
  regimes:
    power_vortex: # ← 缺这段,AI 渲染 fallback 到 current 外观
      look: "twisted rotated blocks pinwheeling around a civic center, sheared glass facades, dynamic swirling skyline, dramatic raking light"
```

---

## ④(可选)`05_完整流程.ipynb` 阶段 C — 展示一格

```python
b = ops.freeze(recs, who=["state"])
plots.operator_demo(b, ops.twist(b, target=["resident","developer"], deg_per_100m=15),
                    "权力漩涡 twist")
```

---

## 跑

```bash
# 05:开 05_完整流程.ipynb → Run All(顶格清模块缓存)
# 06:
cd "06_AI Render Students" && python run.py lujiazui
```

产物:

- `out/lujiazui/Step_05/<时间戳>/` — `regime_compare` / `feature_bars` 多了 `power_vortex`。
- `06/out/lujiazui/canvas.html` · `report.html` — 含 `power_vortex` 体块 + AI 渲染。

---

## 改文件 → 重跑

| 改               | 重跑                         |
| ---------------- | ---------------------------- |
| `operators.py`   | 05 顶格 + 阶段 C;06 `run.py` |
| `regimes.yaml`   | 05 阶段 C/E;06 `run.py`      |
| `06/config.yaml` | 只 06 `run.py <slug>`        |

## 坑

| 症状                | 解                                 |
| ------------------- | ---------------------------------- |
| 06 `KeyError`       | 永久写进 `OPS`(别只 `register`)    |
| 06 没渲染新体制     | `06/config.yaml` `regimes:` 加名字 |
| 06 报告外观不对     | 补 `prompts.regimes.<name>.look`   |
| notebook 改了没生效 | 重跑顶格(清 `sys.modules`)         |
| 几何变空            | 用 `aff.scale`/`aff.rotate`,加下限 |
